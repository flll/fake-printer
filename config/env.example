# fake-printer — generated by scripts/setup.ps1 into paperlessprinter/.env
# See paperlessprinter .env.example for the full option list.

IPP_LISTEN_HOST=0.0.0.0
IPP_LISTEN_PORT=8631
IPP_PATH=/ipp/print
IPP_MAX_BYTES=104857600
IPP_SPOOL_DIR=REPLACE_SPOOL_DIR
IPP_RENDER_DPI=200
IPP_TEMP_DIR=REPLACE_TEMP_DIR
IPP_SEND_EXPECT_CONTINUE=false

LOG_LEVEL=INFO
LOG_HEADERS=false

POST_ENDPOINT=
PAPER_ID=
POST_AUTH_HEADER=x-api-key
POST_AUTH_VALUE=
POST_TIMEOUT_SECONDS=30
POST_FILE_FIELD=file
POST_INCLUDE_META_FIELDS=true
POST_SEND_ALL_PAGES=false

IPP_SHARED_TOKEN=
